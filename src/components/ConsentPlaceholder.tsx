export function ConsentPlaceholder() {
  return (
    <div className="rounded border border-white/10 bg-white/[0.035] p-5 text-sm leading-6 text-muted">
      Analytics and marketing cookies are not active in this implementation. A real
      consent management solution can be connected when those services are configured.
    </div>
  );
}
